{
  "name": "loom-web-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run build --watch",
    "build": "bun build src/main.tsx --outdir ../static --entry-naming '[name].[ext]' --asset-naming '[name].[ext]' --target browser --minify",
    "test": "bun test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.1",
    "typescript": "^5.5.3"
  }
}
